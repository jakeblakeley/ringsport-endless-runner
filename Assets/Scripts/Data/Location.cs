namespace RingSport.Level
{
    // Serialized by index on LevelConfig/LocationConfig assets - only ever
    // append, never reorder or remove, or every existing asset silently
    // re-points at a different world.
    public enum Location
    {
        Arizona,
        Seattle,
        Oregon,
        France,
        Hawaii
    }
}
